#pragma once
#include <vector>
#include <string>
#include <list>
#include <string>
#include <cmath>

inline double norm2(double x, double y, double z = 0)
{
    return sqrt(x * x + y * y + z * z);
}

inline double