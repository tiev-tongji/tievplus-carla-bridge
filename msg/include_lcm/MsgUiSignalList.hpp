/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#ifndef __MsgUiSignalList_hpp__
#define __MsgUiSignalList_hpp__

#include <lcm/lcm_coretypes.h>

#include <vector>
#include "NamedDoubleArray.hpp"
#include "NamedFloatArray.hpp"
#include "NamedIntArray.hpp"
#include "NamedBoolArray.hpp"


class MsgUiSignalList
{
    public:
        int64_t    timestamp;

        int16_t    doubleArraySize;

        std::vector< NamedDoubleArray > doubleArray;

        int16_t    floatArraySize;

        std::vector< NamedFloatArray > floatArray;

        int16_t    intArraySize;

        std::vector< NamedIntArray > intArray;

        int16_t    boolArraySize;

        std::vector< NamedBoolArray > boolArray;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to read while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "MsgUiSignalList"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int MsgUiSignalList::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int MsgUiSignalList::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int MsgUiSignalList::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t MsgUiSignalList::getHash()
{
    static int64_t hash = static_cast<int64_t>(_computeHash(NULL));
    return hash;
}

const char* MsgUiSignalList::getTypeName()
{
    return "MsgUiSignalList";
}

int MsgUiSignalList::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &this->timestamp, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->doubleArraySize, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->doubleArraySize; a0++) {
        tlen = this->doubleArray[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->floatArraySize, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->floatArraySize; a0++) {
        tlen = this->floatArray[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->intArraySize, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->intArraySize; a0++) {
        tlen = this->intArray[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &this->boolArraySize, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->boolArraySize; a0++) {
        tlen = this->boolArray[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int MsgUiSignalList::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this->timestamp, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->doubleArraySize, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->doubleArray.resize(this->doubleArraySize);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->doubleArraySize; a0++) {
        tlen = this->doubleArray[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->floatArraySize, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->floatArray.resize(this->floatArraySize);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->floatArraySize; a0++) {
        tlen = this->floatArray[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->intArraySize, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->intArray.resize(this->intArraySize);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->intArraySize; a0++) {
        tlen = this->intArray[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &this->boolArraySize, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    try {
        this->boolArray.resize(this->boolArraySize);
    } catch (...) {
        return -1;
    }
    for (int a0 = 0; a0 < this->boolArraySize; a0++) {
        tlen = this->boolArray[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    return pos;
}

int MsgUiSignalList::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int64_t_encoded_array_size(NULL, 1);
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->doubleArraySize; a0++) {
        enc_size += this->doubleArray[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->floatArraySize; a0++) {
        enc_size += this->floatArray[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->intArraySize; a0++) {
        enc_size += this->intArray[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int16_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->boolArraySize; a0++) {
        enc_size += this->boolArray[a0]._getEncodedSizeNoHash();
    }
    return enc_size;
}

uint64_t MsgUiSignalList::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == MsgUiSignalList::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, MsgUiSignalList::getHash };

    uint64_t hash = 0x624c56cd34f00e96LL +
         NamedDoubleArray::_computeHash(&cp) +
         NamedFloatArray::_computeHash(&cp) +
         NamedIntArray::_computeHash(&cp) +
         NamedBoolArray::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

#endif
